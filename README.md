# dom-rt
Real-time monitoring framework for distributed operational sites (branches, stores, clinics, depots). Features WebSocket-based live dashboards, append-only audit logging with correlation IDs, role-based access control, and AI-assisted anomaly detection (Isolation Forest). IEEE Access research implementation.
